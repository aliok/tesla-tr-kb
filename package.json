{
  "name": "tesla-tr-kb",
  "version": "1.0.0",
  "description": "Tesla Türkiye Bilgi Bankası - Sık sorulan sorular ve cevapları",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "copy": "mkdir -p dist/pages && cp src/index.html src/styles.css dist/ && cp src/pages/*.html dist/pages/",
    "compile": "tsc",
    "generate-toc": "node scripts/generate-toc.js",
    "build": "npm run clean && npm run copy && npm run compile && npm run generate-toc",
    "dev": "npm run build && npx serve dist"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.31",
    "typescript": "^5.6.0"
  }
}
